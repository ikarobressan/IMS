import 'package:flutter/material.dart';

class SpalshAnimationWidget extends StatelessWidget {
  const SpalshAnimationWidget({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}